## Roadmap issue

Closes #[issue number] (`[Sx-yy]`)

## Problem and fix

- Problem addressed:
- Canonical implementation changed:
- User-visible, schema, migration, privacy, or compatibility impact:

## Duplicate-capability check

- [ ] I searched for an existing implementation of this capability.
- Existing implementation, if any:
- Decision: reuse / extend / replace / deliberately keep separate
- Reason for the decision:

Prefer deleting, reusing, or extending an existing coordinator over adding a parallel implementation.

## Acceptance criteria

- [ ] Every acceptance criterion in the linked issue is complete, or an unresolved item is listed below.
- [ ] The regression test exercises the live behavior rather than relying only on source text.
- [ ] Repository checks, packaged-runtime checks, and external acceptance are reported separately.

## Validation

Exact commands and results:

```text
npm run gate
```

## Deferred work and external gates

- None, or list each intentionally deferred item with a follow-up issue.

Review [the process rules](../docs/PROCESS_RULES.md) and [definition of done](../docs/DEFINITION_OF_DONE.md) before requesting review.
